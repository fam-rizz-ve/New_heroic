# New Heroic Frontend Configuration
# Copy this file to .env and adjust values for your environment.
# Variables must be prefixed with VITE_ to be exposed to client code.

# Backend API URL (FastAPI sidecar) — must match the actual backend host:port
VITE_BACKEND_URL=http://127.0.0.1:1430
